# HTTP application helpers

```{toctree}
:maxdepth: 1

json_get
openapi
static_files
```

The `httk.serve.http` package provides small mountable HTTP applications that
do not require a website source tree. Website rendering and publishing remain
under `httk.serve.web`.
